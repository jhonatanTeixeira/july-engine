global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: july_engine
    static_configs:
      - targets:
          - host.docker.internal:8000   # july_engine running on host
    metrics_path: /metrics
